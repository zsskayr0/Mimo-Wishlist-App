org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false
# Kotlin's newer Build Tools API incremental compiler hits "Could not close
# incremental caches" on this machine (likely Windows real-time AV locking
# the .tab cache files right as Kotlin tries to close them). Incremental
# compilation isn't needed for our from-scratch CI-style builds anyway.
kotlin.incremental=false
